# Math Games Mobile App

## `yarn install`

Install the needed dependencies.

## `yarn start`

Runs the app server in the development mode.

## `yarn sync`

Syncronize the public files with the files in the web application.
